{
  "name": "games",
  "version": "1.0.0",
  "description": "A series of phone-first browser games, one new one a week.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test:all": "node tools/sync-payments.mjs --check && node --test games/lemonade/tests/*.test.mjs && node --test games/outbreak/tests/*.test.mjs && node --test games/the-round/tests/*.test.mjs && node --test shared/payments/tests/*.test.mjs && node --test shared/bonusshop/tests/*.test.mjs",
    "sync": "node tools/sync-payments.mjs"
  },
  "license": "MIT"
}
